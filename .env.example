# Copy to .env and fill in. .env is gitignored — never commit real keys.

# Required only to RE-RUN Signal B (narrative LLM) in 03-narrative-signals.ipynb.
# The derived label parquets are committed, so you can skip this and still
# inspect/rebuild everything downstream of the LLM step.
OPENROUTER_API_KEY=

# Zero-shot model used for the full narrative run (default shown).
OPENROUTER_MODEL=deepseek/deepseek-v4-flash
